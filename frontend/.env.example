# URL pública da API (consumida pelo browser)
NEXT_PUBLIC_API_URL=http://localhost:3001

# URL interna da API (usada pelos rewrites do Next.js — apenas em Docker)
# BACKEND_INTERNAL_URL=http://backend:3001
